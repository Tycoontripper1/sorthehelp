# Base URL of the server/ Express API (see server/.env.example for its own
# config). Only read server-side (in actions/api-request.ts), never exposed
# to the browser.
API_URL="http://localhost:4000/api"
